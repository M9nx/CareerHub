<div data-landing-loader class="landing-loader" aria-hidden="true">
    <p class="swiss-eyebrow">{{ config('app.name', 'CareerHub') }}</p>
    <div class="landing-loader__track" aria-hidden="true">
        <span></span>
    </div>
    <p class="text-xs opacity-40">{{ __('Loading…') }}</p>
</div>
